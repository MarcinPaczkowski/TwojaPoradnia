import React from 'react';
import './negativeMarginWrapper.scss';

const NegativeMarginWrapper = ({ children }) => (
  <div className="negative-margin-wrapper">{children}</div>
);

export default NegativeMarginWrapper;
